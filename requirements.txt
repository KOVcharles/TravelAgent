# 环境变量加载
python-dotenv==1.0.1

# 基础（milvus_lite 依赖 pkg_resources，需 setuptools<82）
setuptools>=69.0.0,<82

# 多智能体框架
agentscope==1.0.16

# LLM 调用
requests==2.32.3
httpx==0.28.1

# RAG 与向量库（milvus-lite 显式安装，用于本地向量库）
pymilvus==2.6.9
milvus-lite
sentence-transformers==5.2.3
pypdf==6.4.1

# CLI 界面
rich==13.9.4

# FastAPI WebUI
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
# mcp>=1.13（agentscope 1.0.16 的依赖）要求 pydantic>=2.11.0，故放宽下限
pydantic>=2.11.0,<3.0.0

# 网络搜索（DDGS）
ddgs==9.10.0

# 短期记忆 Redis 存储
#redis==6.4.0
redis>=5.0.0,<7.0.0

# 长期记忆 PostgreSQL 存储
psycopg[binary]

# 鉴权系统（JWT + bcrypt + 邮箱校验）
PyJWT>=2.8
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<4.1          # 收紧上限以规避 passlib 1.7 与 bcrypt≥4.1 的兼容告警（见 questions.md Q-F）
email-validator>=2.0      # pydantic v2 EmailStr 所需

# Test runner
pytest==8.3.4

# Skill metadata parsing
PyYAML==6.0.2
