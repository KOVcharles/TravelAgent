# Local models

Place local embedding or LLM model files in this directory.

Model weights are ignored by Git because they are large runtime artifacts.
Keep download instructions or model names here instead of committing weights.
