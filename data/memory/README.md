# Local memory

Runtime user memory files live in this directory.

JSON memory snapshots are ignored by Git because they may contain private user
information. Keep only this README and `.gitkeep` in version control.
