# ============================================================
# Docker 生产环境最小依赖
# 仅包含 webui (FastAPI) 运行时真正需要的包
# ============================================================

# 环境变量加载
python-dotenv==1.0.1

# milvus-lite 依赖 pkg_resources
setuptools>=69.0.0,<82

# 多智能体框架
agentscope==1.0.16

# HTTP 调用
requests==2.32.3
httpx==0.28.1

# RAG 与向量库（embedding 默认走 SiliconFlow 云端，不在容器内加载本地 BGE/PyTorch）
pymilvus==2.6.9
milvus-lite
pypdf==6.4.1

# FastAPI WebUI
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
pydantic>=2.11.0,<3.0.0

# CLI 界面
rich==13.9.4

# 网络搜索（可选，导入失败时优雅降级）
ddgs==9.10.0

# Skill 元数据解析
PyYAML==6.0.2

# 短期记忆 Redis 后端
redis>=5.0.0,<7.0.0

# 长期记忆 PostgreSQL 后端
psycopg[binary]
